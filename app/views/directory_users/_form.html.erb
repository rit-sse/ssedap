<%= form_for(@directory_user) do |f| %>
  <% if @directory_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@directory_user.errors.count, "error") %> prohibited this directory_user from being saved:</h2>

      <ul>
      <% @directory_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :role %><br />
    <%= f.collection_select :role_id, Role.order_by(:name, :asc), :id, :name, include_blank: "None" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
